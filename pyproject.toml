[project]
name = "lhc-mechanism-audit"
version = "0.1.0"
description = "Mechanism-first audit of LHC microscopic black-hole safety literature."
requires-python = ">=3.9"
dependencies = [
  "matplotlib>=3.5",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
